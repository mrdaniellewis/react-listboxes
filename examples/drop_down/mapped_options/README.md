# Drop down with mapped options

A drop down using `mapOption` to transform options into the correct format.

It is recommended that `mapOption` is a constant.  Either define it outside of the component,
or `useCallback`.
