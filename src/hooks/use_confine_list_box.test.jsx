import React from 'react';

describe('maxWidth', () => {
  it.todo('adds maxWidth if the listbox is too wide');
  it.todo('does not add maxWidth if the listbox is not too wide');
});

describe('maxHeight', () => {
  it.todo('adds maxHeight if the listbox is too high');
  it.todo('does not add maxHeight if the listbox is not too high');
});

